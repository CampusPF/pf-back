# @nestjs/throttler 6.x todavia declara peerDependency solo hasta @nestjs/common
# v11, y este proyecto usa v12. La API es compatible, es solo un rango de
# peerDependency desactualizado en el paquete. Sin esto, `npm ci` en hosts
# como Render falla con ERESOLVE.
legacy-peer-deps=true
